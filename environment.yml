name: tdnr
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip

  - pip:
      # Core HF and TRL
      - transformers>=4.57.0
      - trl>=0.25.0
      - datasets>=4.4.0
      - accelerate>=1.12.0
      - peft>=0.18.0
      - safetensors>=0.6.0
      - tokenizers>=0.22.0
      - sentencepiece>=0.2.0

      # Utilitiesfor datasets and training
      - numpy>=2.0.0
      - pandas>=2.0.0
      - pyarrow>=12.0.0
      - tqdm>=4.60.0
      - pyyaml>=6.0.0
      - regex>=2024.0.0
      - requests>=2.31.0

      # Experiment tracking
      - wandb>=0.23.0

      # API clients for models (check if still in use)
      #- openai>=2.0.0
      #- google-api-python-client>=2.0.0
      #- google-cloud-translate>=3.0.0

      # might need to add strongreject
